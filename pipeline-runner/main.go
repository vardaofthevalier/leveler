package main 

import (
	"fmt"
)

func main() {
	// initialize test database (sqlite)
	// create an admin account

	// read in test CICD pipeline config
	// build CICD pipeline
	// run CICD pipeline

	// apply constraints to admin account
	// rerun pipeline to test constraints

	// read in test batch pipeline config
	// build batch pipeline
	// run batch pipeline
}